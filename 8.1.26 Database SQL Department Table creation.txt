create database CGIF;
create database Wipro;
use CGIF;
create table TB1(
cgid INT,
Name varchar(255),
Address varchar(255)
City varchar(255)
);

create table Department(
id INT,

Branch varchar(255),
Location varchar(255)
);

create table student(
sid int,
Firstname varchar(255),
Lastname varchar(255),
Address varchar(255),
City varchar(255)
);

use Wipro
show tables;

use CGIF;
show tables;
INSERT INTO student(sid,Firstname,Lastname,Address,City) values(1,'Ajay','More','Kothrud','Pune');
select * from student